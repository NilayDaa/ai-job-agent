version: "3.9"

services:
  ai-job-agent:
    build: .
    container_name: ai-job-agent

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads

    restart: unless-stopped